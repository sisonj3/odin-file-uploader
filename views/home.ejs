<html>
    <body>
        <% if (locals.name) {%>
            <h1>Hello, <%= name %>!</h1>
        <% } else { %>
            <h1>Hello, World!</h1>
        <% } %>

        <button onclick="location.href='/log-in/log-out';">Log Out</button>
    </body>
</html>